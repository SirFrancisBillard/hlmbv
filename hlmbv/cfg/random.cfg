// Pseudo-randomness, each of these are called on W, A, S, and D respectively
// These are just declared as virtual here so we aren't calling a non-existant command so often

alias random_1
alias random_2
alias random_3
alias random_4
